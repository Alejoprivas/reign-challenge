import { DateChangerPipe } from './date-changer.pipe';

describe('DateChangerPipe', () => {
  it('create an instance', () => {
    const pipe = new DateChangerPipe();
    expect(pipe).toBeTruthy();
  });
});
